<%= form_for [:admin, @government] do |f| %>
  <% if @government.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@government.errors.count, "error") %> prohibited this government from being saved:</h2>

      <ul>
      <% @government.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<table>
	<tr>
		<td><%= f.label :name %></td>
		<td><%= f.text_field :name %></td>
	</tr>
	<tr>
		<td><%= f.label :logo %></td>
		<td><%= f.text_field :logo %></td>
	</tr>

	<tr>
		<td><%= f.label :social_score %></td>
		<td><%= f.text_field :social_score %></td>
	</tr>
	
	<tr>
		<td><%= f.label :participation_rate %></td>
		<td><%= f.text_field :participation_rate %></td>
	</tr>
</table>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
